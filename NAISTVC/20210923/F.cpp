// Give up to implement in time
#include <bits/stdc++.h>

using namespace std;

int main() {
    string Hi, Wi;
    cin >> Hi >> Wi;
    int H = stoi(Hi);
    int W = stoi(Wi);
    int orbs[H]{};
    int ingots[W]{};
    vector<tuple<int, int, int, int>> jewels;

    for(int hs = 0; hs < H; hs++){
        string x;
        cin >> x;
        int orbcnt = 0;
        for(int ws = 0; ws < W; ws++){
            char v = x[ws];
            if v == 'J'{
                jewels.push_back()
            }
        }
    }
}